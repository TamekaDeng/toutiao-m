import Vue from 'vue'
import Vuex from 'vuex'

Vue.use(Vuex)
// 创建了一个Store容器的实例
export default new Vuex.Store({
  // 在里面放了下面这些东西，将来如果需要通过这个容器来管理那些需要共享的数据，
  // 只需往里填充你的业务代码

  // 这是容器中初始的结构
  state: {
  },
  mutations: {
  },
  actions: {
  },
  modules: {
  }
})
